import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.audiodarsliklar.app',
  appName: 'Audio Darsliklar 2026',
  webDir: 'dist'
};

export default config;
